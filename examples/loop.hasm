# simple loop

main:
    push 10        
    jmp loop

loop:
    dup 0
    dump           

    push 1         
    minusi

    dup 0
    push 0
    eqi
    jnz exit       

    jmp loop

exit:
    halt